# 刷题网页 — 实现方案

## 概述

做一个纯静态的单页刷题 Web App（单个 HTML 文件 + 一个 questions.json 数据文件），用户自己按照设计好的 JSON 格式整理题目后即可直接用浏览器打开使用。

---

## 第一步：设计 JSON 数据格式

文件名：`questions.json`

```json
{
  "meta": {
    "total_exams": 8,
    "high_freq_threshold": 3
  },
  "questions": [
    {
      "id": 1,
      "type": "single",
      "stem": "下列说法正确的是？",
      "options": {
        "A": "选项A",
        "B": "选项B",
        "C": "选项C",
        "D": "选项D"
      },
      "answer": "A",
      "frequency": 5,
      "explanation": "（可选）解析文字"
    },
    {
      "id": 2,
      "type": "multiple",
      "stem": "以下哪些属于……",
      "options": {
        "A": "选项A",
        "B": "选项B",
        "C": "选项C",
        "D": "选项D"
      },
      "answer": "AC",
      "frequency": 2,
      "explanation": ""
    },
    {
      "id": 3,
      "type": "truefalse",
      "stem": "……说法正确。",
      "options": null,
      "answer": "true",
      "frequency": 6,
      "explanation": ""
    }
  ]
}
```

字段说明：
- `type`：`"single"` 单选 / `"multiple"` 多选 / `"truefalse"` 判断
- `answer`：单选填字母如 `"A"`；多选填字母串如 `"AC"`；判断填 `"true"` 或 `"false"`
- `frequency`：该题在历年真题中出现的次数
- `meta.high_freq_threshold`：出现次数 ≥ 此值则标记为高频（默认 3）

---

## 第二步：实现 quiz.html（单文件 Web App）

### 布局

```
┌──────────────────────────────┐
│  顶栏：进度条 + 进度文字       │
├──────────────────────────────┤
│  筛选栏：                     │
│  [全部] [高频] [低频]          │
│  [单选] [多选] [判断]          │
│  [乱序] [仅未掌握]             │
├──────────────────────────────┤
│  题目卡片：                   │
│   ┌ 徽章：[单选] [高频🔥]      │
│   │ 题干文字                   │
│   │ A. ……   B. ……             │
│   │ C. ……   D. ……             │
│   │ （判断题：[正确] [错误]）  │
│   └─────────────────────────  │
│  [显示答案] 按钮               │
│  ↓ 点击后显示：               │
│   正确答案：A                  │
│   解析：……（如有）            │
├──────────────────────────────┤
│  底部：[← 上一题] [已掌握✓]   │
│         [未掌握✗] [下一题 →]  │
└──────────────────────────────┘
```

### 功能点

1. **题目展示**
   - 单选题：显示 A/B/C/D 按钮，点击选中高亮
   - 多选题：多个选项可选中，最终提交
   - 判断题：显示「正确」/「错误」两个按钮
   - 答案默认隐藏

2. **显示答案**
   - 点击「显示答案」后揭示正确答案
   - 如果用户已选择选项，正确选项变绿，错误选项变红
   - 有解析则一并展示

3. **频率标记**
   - frequency ≥ high_freq_threshold → 显示「高频🔥」红色徽章
   - 否则显示「低频」灰色徽章

4. **掌握状态**（持久化到 localStorage）
   - 每道题可标记「已掌握」或「未掌握」
   - 可按「仅未掌握」筛选刷题

5. **筛选 & 乱序**
   - 按频率筛选：全部 / 高频 / 低频
   - 按题型筛选：全部 / 单选 / 多选 / 判断
   - 乱序模式：随机打乱当前题目列表顺序
   - 筛选条件可组合

6. **进度追踪**
   - 顶部显示「当前题/总题数」和进度条
   - 已掌握题目数量统计

---

## 第三步：提供一个样例 questions.json

包含 ~10 道各类型示例题，方便用户理解格式后自行填入真实题目。

---

## 文件结构

```
testing123/
├── quiz.html          ← 刷题 Web App（双击即可用浏览器打开）
├── questions.json     ← 题库数据（用户填充真实题目）
└── questions.sample.json  ← 示例数据，方便参考格式
```

---

## 实现顺序

1. 创建 `questions.sample.json`（示例数据）
2. 创建 `questions.json`（同结构空模板）
3. 实现 `quiz.html`（完整功能）
4. 推送到 `claude/exam-quiz-webapp-RFvwn` 分支
